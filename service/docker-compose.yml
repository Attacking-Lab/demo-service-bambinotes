name: bambinotes

services:
  bambinotes:
    container_name: bambinotes
    build: .
    volumes:
      - ./data/:/service/data:rw
    ports:
      - 9204:8204

networks:
  default:
    name: bambinotes
