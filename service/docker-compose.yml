name: bambinotes

services:
  bambinotes:
    container_name: bambinotes
    build: .
    volumes:
      - ./data/:/service/data:rw
    ports:
      - 9204:8204
    restart: unless-stopped

networks:
  default:
    name: bambinotes
